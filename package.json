{
  "name": "meraki",
  "version": "0.2.0",
  "private": true,
  "description": "Firefox extension: highlight/annotate any page, export to org-mode.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild src/content.js --bundle --format=iife --target=firefox140 --outfile=extension/content.js",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "web-ext": "^10.4.0"
  }
}
